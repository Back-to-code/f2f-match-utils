# Tokenizer

This package provides a tokenizer that converts text into text tokens.

This package is intented to be used to fuckeduply fast match fuckedup amounts of text as comparing numbers is fuckedup faster.
